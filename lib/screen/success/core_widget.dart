import 'package:flutter/material.dart';

class SuccsessPage extends StatelessWidget {
  const SuccsessPage({super.key});

  @override
  Widget build(BuildContext context) => const Center(child: Text('Success'));
}
